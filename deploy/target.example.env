# Copy to deploy/target.env (gitignored) and set the production Droplet SSH target.
# The deploy command verifies DigitalOcean metadata on the remote host before any write.
ZEN_DEPLOY_PROVIDER=digitalocean
ZEN_DEPLOY_SSH_TARGET=root@203.0.113.10
