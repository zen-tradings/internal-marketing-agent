# Zen Content Hub — 部署环境变量样例（不含真实值，复制为 .env 并填写）
# 用法：cp deploy/.env.example /srv/zen/app/.env 然后填入真实值，权限建议 chmod 600 .env

# ---- 运行路径 ----
WORK_DIR=/srv/zen/wechat
DB_PATH=/srv/zen/runs.db
CLAUDE_BIN=/root/.local/bin/claude

# ---- 并发/超时 ----
MAX_CONCURRENCY=1
DEFAULT_TIMEOUT_MS=600000

# ---- 代理：只给 Claude 子进程用（访问 Anthropic/Exa 等境外服务）----
# 主进程（微信 API 直连所必需）严禁设置 https_proxy/http_proxy/all_proxy，
# 见 src/index.js 的 assertMainProcessDirect() 强制校验；代理只能通过下面的 CHILD_* 变量注入子进程。
CHILD_HTTPS_PROXY=http://127.0.0.1:7897
CHILD_ALL_PROXY=socks5://127.0.0.1:7897
# 微信域名必须绕过代理，直连大陆网络，否则会被判定为境外 IP 触发风控/延迟
NO_PROXY=api.weixin.qq.com,mp.weixin.qq.com

# ---- Slack ----
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
NOTIFY_CHANNEL_ID=

# ---- 微信公众号 ----
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# ---- 发布渠道 ----
# wechat 工作流实际使用的渠道,默认 wechat-draft(真实发布到微信草稿箱)。
# 本地/演练环境想避免真的发草稿,可设为 mock,或直接设 HUB_DRY_RUN=1
# (HUB_DRY_RUN 优先级更高,不管 WECHAT_CHANNEL 是什么都会强制走 mock)。
WECHAT_CHANNEL=wechat-draft
# HUB_DRY_RUN=1
